export const initialState = Object.freeze({
  time: 30,
  lives: 3
});

export const currentState = {
  time: 30,
  lives: 3
};

export const stats = {
  wrong: `wrong`,
  correct: `correct`,
  slow: `slow`,
  fast: `fast`,
  unknown: `unknown`
};
